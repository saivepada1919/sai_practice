#include <stdio.h>
int main(){
	float a,b,difference;
	printf("enter the two numbers to find difference= ");
	scanf("%f%f",&a,&b);
	difference=a-b;
	printf("difference between two numbers=%.2f\n",difference);
	return 0;
}
